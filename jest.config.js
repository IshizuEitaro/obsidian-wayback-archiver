/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // Optional: Specify test file pattern if needed (e.g., if tests are in a specific folder)
  // testMatch: ['**/__tests__/**/*.test.ts'],
};